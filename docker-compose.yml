name: lamalda-mysql

version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: lamalda-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: nicoRoot2025
      MYSQL_DATABASE: lamalda_db
      MYSQL_USER: lamalda_admin
      MYSQL_PASSWORD: tz9WJVCZXHBt4m
    ports:
      - "3306:3306"
    command: >
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
    volumes:
      - ./mysql_data:/var/lib/mysql
